# Dotfiles

My configuration files for Linux
cd ~
git clone https://github.com/JoeyDeSmet/dotfiles.git --recursive
cd dotfiles
./install
## How to

```bash
cd ~
git clone https://github.com/JoeyDeSmet/dotfiles.git --recursive
cd dotfiles
./install
```